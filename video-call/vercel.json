{
  "version": 2,
  "builds": [
    { "src": "dist/**/*", "use": "@vercel/static-build" },
    { "src": "server/server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/server.mjs" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
